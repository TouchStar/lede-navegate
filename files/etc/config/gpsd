config gpsd core
    option device    "/dev/ttyGPS2"
    option port    "2947"
    option listen_globally    "true"   # this is critical at boot time (otherwise gpsd can't bind to loop back).
    option enabled    "false"
